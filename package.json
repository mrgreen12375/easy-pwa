{
  "name": "easy-pwa",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start:dev": "concurrently \"cd public && npm run dev\"",
    "start": "npm run build && node server.js",
    "build": "cd public && npm run build",
    "install": "cd ./public && npm i",
    "public": "cd public && npm start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "nodemon": "^2.0.4"
  }
}
